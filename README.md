# 📝 Лабораторная работа №1: HTTP-методы в React

## Описание проекта

Это приложение для управления постами, разработанное в рамках лабораторной работы по изучению работы с HTTP-методами (GET, POST, PUT, DELETE) в React с использованием библиотеки Axios.

## Технологии

- **React 19.2.0** - библиотека для построения пользовательского интерфейса
- **Vite** - инструмент для сборки и разработки
- **Axios** - библиотека для HTTP-запросов
- **JSONPlaceholder API** - тестовый REST API

## Функциональность

### ✅ Реализованные HTTP-методы

1. **GET-запрос**
   - Загрузка списка постов при монтировании компонента
   - Отображение данных в интерфейсе
   - Кнопка "Обновить" для повторной загрузки

2. **POST-запрос**
   - Форма для создания нового поста
   - Добавление поста в список на фронтенде
   - Валидация полей формы

3. **PUT-запрос**
   - Редактирование существующего поста
   - Обновление данных в реальном времени
   - Inline-редактирование с кнопками "Сохранить" и "Отмена"

4. **DELETE-запрос**
   - Удаление поста с подтверждением
   - Обновление интерфейса после удаления

### 🎨 Дополнительные возможности

- **Обработка ошибок** - отображение сообщений об ошибках с возможностью повторить запрос
- **Индикатор загрузки** - анимированный спиннер во время выполнения запросов
- **Уведомления** - всплывающие сообщения об успешных операциях
- **Адаптивный дизайн** - корректное отображение на разных устройствах

## Структура проекта

```
src/
├── api.js                          # Модуль для работы с API
├── App.jsx                         # Главный компонент
├── App.css                         # Стили главного компонента
├── components/
│   ├── PostForm.jsx                # Форма создания поста
│   ├── PostForm.css
│   ├── PostItem.jsx                # Карточка поста
│   ├── PostItem.css
│   ├── LoadingSpinner.jsx          # Индикатор загрузки
│   ├── LoadingSpinner.css
│   ├── ErrorMessage.jsx            # Компонент ошибки
│   └── ErrorMessage.css
```

## Установка и запуск

1. Установите зависимости:
```bash
npm install
```

2. Запустите dev-сервер:
```bash
npm run dev
```

3. Откройте приложение в браузере по адресу: `http://localhost:5173`

## API Endpoints

Используется JSONPlaceholder API:
- GET `/posts` - получение всех постов
- GET `/posts/:id` - получение поста по ID
- POST `/posts` - создание поста
- PUT `/posts/:id` - полное обновление поста
- PATCH `/posts/:id` - частичное обновление поста
- DELETE `/posts/:id` - удаление поста

## Особенности реализации

### Модуль API (src/api.js)

Все функции для работы с API вынесены в отдельный модуль. Используется axios с базовым URL и обработкой ошибок:

```javascript
import axios from 'axios';

const api = axios.create({
  baseURL: 'https://jsonplaceholder.typicode.com',
  headers: {
    'Content-Type': 'application/json',
  },
});
```

### Обработка состояний

В приложении используется три основных состояния:
- `loading` - индикатор загрузки
- `error` - информация об ошибке
- `posts` - массив постов

### UX-улучшения

- Подтверждение удаления через `window.confirm()`
- Автоматическое скрытие уведомлений через 3 секунды
- Ограничение отображаемых постов (10 шт.) для удобства

## Git коммиты

Все изменения зафиксированы в репозитории с понятными сообщениями:
- `feat: add axios and API module`
- `feat: add GET request for posts`
- `feat: add POST request for creating posts`
- `feat: add PUT request for updating posts`
- `feat: add DELETE request`
- `feat: add error handling and loading spinner`
- `style: add responsive design`

## Скриншоты

> Для полного отчета необходимо сделать скриншоты:
> 1. Главная страница с загруженными постами
> 2. Форма создания нового поста
> 3. Процесс редактирования поста
> 4. Индикатор загрузки
> 5. Сообщение об успешной операции
> 6. Обработка ошибки

## Автор

Выполнено в рамках лабораторной работы по программированию

## Дата выполнения

18 декабря 2025 г.

